apiVersion: v1
kind: Service
metadata:
  namespace: "{$PROJECTNAME}"
  name: "mysql-fw"
spec:
  selector:
    app: "db"
  type: LoadBalancer
  ports:
    - name: mysql
      port: {$MYSQL_PORT}
      targetPort: mysql